<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.ravito</groupId>
    <artifactId>ravito-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Ravito</name>
    <description>Planification de repas hebdomadaire et generation de liste de courses</description>

    <modules>
        <module>domain</module>
        <module>application</module>
        <module>infrastructure-persistence</module>
        <module>infrastructure-web</module>
        <module>bootstrap</module>
    </modules>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <junit-jupiter.version>5.10.2</junit-jupiter.version>
        <assertj.version>3.26.3</assertj.version>
        <mockito.version>5.11.0</mockito.version>
        <spring-boot.version>3.3.5</spring-boot.version>
        <testcontainers.version>1.21.4</testcontainers.version>

        <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>3.2.5</maven-surefire-plugin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Importee avant spring-boot-dependencies : notre version de JUnit prime
                 sur celle que Spring Boot gererait sinon (premiere declaration gagnante
                 en cas de conflit sur le meme GA, voir la doc Maven BOM). -->
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit-jupiter.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.assertj</groupId>
                <artifactId>assertj-core</artifactId>
                <version>${assertj.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-core</artifactId>
                <version>${mockito.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-junit-jupiter</artifactId>
                <version>${mockito.version}</version>
                <scope>test</scope>
            </dependency>
            <!-- Importee avant spring-boot-dependencies, pour la meme raison que
                 junit-bom : Spring Boot 3.3.5 gere Testcontainers 1.19.8, dont le
                 client Docker (docker-java) ne sait pas discuter avec les versions
                 recentes de Docker Desktop (erreur HTTP 400 sur le named pipe). On
                 prend une version plus recente explicitement. -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Gere les versions de Spring Data JPA, Hibernate, driver PostgreSQL,
                 Flyway, etc. pour tous les modules qui en ont besoin, sans faire de
                 ravito-parent un heritier de spring-boot-starter-parent (on garde
                 notre propre POM racine). -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                </plugin>
                <!-- Failsafe (pas Surefire) execute les tests d'integration : convention
                     de nommage *IT.java, phases integration-test/verify. Ca separe les
                     tests unitaires rapides (mvn test) des tests Testcontainers, plus
                     lents (mvn verify) car ils demarrent un vrai conteneur Docker. -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                    <executions>
                        <execution>
                            <goals>
                                <goal>integration-test</goal>
                                <goal>verify</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
